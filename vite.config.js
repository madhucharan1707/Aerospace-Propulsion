import { defineConfig } from 'vite'

export default defineConfig({
    // 'base' is configured for GitHub Pages.
    // If you are deploying to https://<USERNAME>.github.io/<REPO>/
    // then base should be '/<REPO>/'.
    // Using './' allows relative paths, which usually works for simple deployments too.
    base: './',
})
